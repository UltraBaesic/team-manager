User Types: member, manager, admin.

only one user can be a team leader
admin can create teams and assign team leaders
tasks load based on the date selected and if left empty, the date is set to "today"
once a task's status is changed to "doing" or "done", it would be seen only under that status.